apiVersion: v1
kind: Service
metadata:
  name: {{ include "stakater-cloudnative-pg.fullname" . }}
  labels:
    {{- include "stakater-cloudnative-pg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stakater-cloudnative-pg.selectorLabels" . | nindent 4 }}
